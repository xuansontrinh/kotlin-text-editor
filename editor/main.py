from TextEditor import TextEditor

if __name__ == '__main__':
    # Init the text editor with language kotlin and theme dracula
    text_editor = TextEditor(language='kotlin', theme='dracula')
    text_editor.render()
